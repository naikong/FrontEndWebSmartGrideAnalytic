version: "3.9"

services:
  react:
    container_name: WebSmartGrideDataAnalytic
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
